import { createTheme } from "@material-ui/core";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#111",
        },
        secondary: {
            main: "#fff",
        },
    },
    myButton: {
        color: "yellow",
        backgroundColor: "black"
    }
});